# Servidor
PORT=3001
NODE_ENV=production

# Seguridad - genera una clave fuerte (ej: openssl rand -base64 32)
JWT_SECRET=reemplaza-con-una-clave-secreta-muy-larga-y-aleatoria

# Stripe - usa tus claves de Stripe
STRIPE_SECRET_KEY=tu_clave_secreta_de_stripe
STRIPE_WEBHOOK_SECRET=tu_webhook_secret_de_stripe

# URL de tu app en Railway (se conoce despues de crear el servicio)
CLIENT_URL=https://tu-app.up.railway.app
