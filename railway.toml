[build]
buildCommand = "npm run install:server && cd server && npm rebuild better-sqlite3 && cd .. && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
